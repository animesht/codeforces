#include <stdio.h>
#include <conio.h>
#define x 1000000000
//---------------------------------------------------------------------------

int main()
{
 __int64 n,m,a,z;
 n=0;
 m=0;
 a=0;
 z=1;
 while((n<1)||(n>x)||(m<1)||(m>x)||(a<1)||(a>x))
 {
  scanf("%d %d %d",&n,&m,&a);
 }
//------------------------------------------------------------------------------
 if (m%a==0)
   z=z*(m/a);
   else
   z=z*((m/a)+1);
 if (n%a==0)
   z=z*(n/a);
   else
   z=z*((n/a)+1);
 printf("%I64d",z);
 return 0;
}